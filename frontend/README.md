# Frontend (React + Vite + Tailwind)

From the `frontend` directory:

Install dependencies:

```bash
npm install
```

Run development server:

```bash
npm run dev
```

The app expects the backend FastAPI to be running at `http://localhost:8000` and will call `/health`.
